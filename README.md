# Database-Student-Management-SQL
This project is a Student Management System built using SQL Server. It demonstrates core database concepts such as table creation, relationships, CRUD operations, transactions (COMMIT and ROLLBACK), triggers for auditing changes, and stored procedures for efficient data management.
